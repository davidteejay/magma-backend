import express from 'express';

const router = express.Router();

router.get('/user', () => {

});

router.put('/user', () => {

});

router.post('/users/login', () => {

});

router.post('/users', () => {

});

export default router;
